<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .glass-effect {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
        }
        
        .float-animation {
            animation: float 6s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
        
        .whatsapp-green {
            background: linear-gradient(135deg, #25d366 0%, #128c7e 100%);
        }
        
        .whatsapp-green:hover {
            background: linear-gradient(135deg, #128c7e 0%, #0d7377 100%);
        }
        
        .pulse-whatsapp {
            animation: pulse-whatsapp 2s infinite;
        }
        
        @keyframes pulse-whatsapp {
            0%, 100% { 
                transform: scale(1);
                box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.7);
            }
            50% { 
                transform: scale(1.05);
                box-shadow: 0 0 0 10px rgba(37, 211, 102, 0);
            }
        }
    </style>
</head>
<body class="gradient-bg min-h-screen flex items-center justify-center">
    <div class="container mx-auto px-4">
        <div class="max-w-md mx-auto">
            <!-- Logo y título -->
            <div class="text-center mb-10">
                <div class="inline-block p-6 bg-white rounded-full shadow-2xl mb-6 float-animation pulse-whatsapp">
                    <i class="fab fa-whatsapp text-5xl text-green-500"></i>
                </div>
                <h1 class="text-5xl font-bold text-white mb-3 tracking-tight">WhatsaBoot</h1>
                <p class="text-white text-xl opacity-90 font-light">Sistema de Gestión de Bots de WhatsApp</p>
                <div class="mt-4 flex justify-center space-x-2">
                    <span class="w-2 h-2 bg-white rounded-full opacity-60"></span>
                    <span class="w-2 h-2 bg-white rounded-full opacity-80"></span>
                    <span class="w-2 h-2 bg-white rounded-full"></span>
                </div>
            </div>
            
            <!-- Formulario de login -->
            <div class="glass-effect rounded-3xl p-8 shadow-2xl">
                <div class="text-center mb-6">
                    <h2 class="text-2xl font-semibold text-white mb-2">Bienvenido</h2>
                    <p class="text-white opacity-80 text-sm">Inicia sesión para gestionar tus bots de WhatsApp</p>
                </div>
                
                <% if (typeof error !== 'undefined' && error) { %>
                <div class="bg-red-500 bg-opacity-20 border border-red-400 text-white px-4 py-3 rounded-lg mb-6">
                    <div class="flex items-center">
                        <i class="fas fa-exclamation-triangle mr-2"></i>
                        <span>
                            <% if (error === 'auth_required') { %>
                                Debes iniciar sesión para acceder.
                            <% } else if (error === 'auth_failed') { %>
                                Error en la autenticación. Intenta nuevamente.
                            <% } else { %>
                                <%= error %>
                            <% } %>
                        </span>
                    </div>
                </div>
                <% } %>
                
                <div class="space-y-6">
                    <p class="text-white text-center opacity-90">
                        Usa tu cuenta de Google para acceder al sistema de gestión de bots de WhatsApp.
                    </p>
                    
                    <a href="/auth/google" class="w-full bg-white hover:bg-gray-50 text-gray-700 font-semibold py-4 px-6 rounded-xl shadow-xl transition-all duration-300 flex items-center justify-center group transform hover:scale-105 hover:shadow-2xl">
                        <svg class="w-5 h-5 mr-3" viewBox="0 0 24 24">
                            <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                            <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                            <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                            <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                        </svg>
                        <span>Continuar con Google</span>
                        <i class="fas fa-arrow-right ml-3 opacity-0 group-hover:opacity-100 transition-all duration-300 transform group-hover:translate-x-1"></i>
                    </a>
                </div>
                
                <div class="mt-8 pt-6 border-t border-white border-opacity-20">
                    <div class="text-center text-white text-sm opacity-75">
                        <p>¿Primera vez aquí?</p>
                        <p class="mt-1">El primer usuario registrado será administrador</p>
                    </div>
                </div>
            </div>
            
            <!-- Features preview -->
            <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-6 text-center">
                <div class="glass-effect rounded-xl p-6 transform hover:scale-105 transition-all duration-300 hover:shadow-2xl">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-600 rounded-xl mb-4">
                        <i class="fas fa-robot text-white text-xl"></i>
                    </div>
                    <h3 class="text-white font-semibold mb-2 text-lg">Bots Inteligentes</h3>
                    <p class="text-white text-sm opacity-75 leading-relaxed">Respuestas automáticas con IA y palabras clave personalizables</p>
                </div>
                
                <div class="glass-effect rounded-xl p-6 transform hover:scale-105 transition-all duration-300 hover:shadow-2xl">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-gradient-to-r from-green-500 to-teal-600 rounded-xl mb-4">
                        <i class="fas fa-users text-white text-xl"></i>
                    </div>
                    <h3 class="text-white font-semibold mb-2 text-lg">Multi-Usuario</h3>
                    <p class="text-white text-sm opacity-75 leading-relaxed">Gestión de múltiples usuarios y números de WhatsApp</p>
                </div>
                
                <div class="glass-effect rounded-xl p-6 transform hover:scale-105 transition-all duration-300 hover:shadow-2xl">
                    <div class="inline-flex items-center justify-center w-12 h-12 bg-gradient-to-r from-yellow-500 to-orange-600 rounded-xl mb-4">
                        <i class="fas fa-chart-line text-white text-xl"></i>
                    </div>
                    <h3 class="text-white font-semibold mb-2 text-lg">Analytics</h3>
                    <p class="text-white text-sm opacity-75 leading-relaxed">Estadísticas y métricas en tiempo real</p>
                </div>
            </div>
            
            <!-- Footer -->
            <footer class="mt-12 text-center text-white text-sm opacity-60">
                <p>&copy; 2024 WhatsaBoot. Desarrollado con ❤️ para la automatización de WhatsApp.</p>
            </footer>
        </div>
    </div>
    
    <!-- Particles background effect -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute -top-10 -left-10 w-72 h-72 bg-white opacity-5 rounded-full blur-3xl"></div>
        <div class="absolute top-1/2 -right-10 w-96 h-96 bg-white opacity-5 rounded-full blur-3xl"></div>
        <div class="absolute -bottom-10 left-1/3 w-80 h-80 bg-white opacity-5 rounded-full blur-3xl"></div>
    </div>
    
    <script>
        // Show loading on Google auth click
        document.querySelector('a[href="/auth/google"]').addEventListener('click', function() {
            this.innerHTML = '<i class="fas fa-spinner fa-spin mr-3"></i>Conectando...';
            this.classList.add('opacity-75', 'cursor-not-allowed');
        });
        
        // Auto-hide error messages
        const errorDiv = document.querySelector('.bg-red-500');
        if (errorDiv) {
            setTimeout(() => {
                errorDiv.style.opacity = '0';
                setTimeout(() => errorDiv.remove(), 300);
            }, 5000);
        }
    </script>
</body>
</html>
