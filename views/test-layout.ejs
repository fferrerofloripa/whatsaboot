<div class="p-8">
    <h1 class="text-2xl font-bold text-gray-800">Test Layout</h1>
    <p class="text-gray-600 mt-4">Este es un test para verificar que el layout funciona correctamente.</p>
    
    <div class="mt-8 bg-white rounded-lg shadow p-6">
        <h2 class="text-lg font-semibold mb-4">Información del usuario</h2>
        <p><strong>Usuario:</strong> <%= user ? user.displayName : 'No autenticado' %></p>
        <p><strong>Email:</strong> <%= user ? user.email : 'N/A' %></p>
        <p><strong>Rol:</strong> <%= user ? user.role : 'N/A' %></p>
    </div>
    
    <div class="mt-4">
        <a href="/crm" class="inline-block px-4 py-2 bg-primary-500 text-white rounded-lg hover:bg-primary-600">
            Ir al CRM
        </a>
    </div>
</div>
