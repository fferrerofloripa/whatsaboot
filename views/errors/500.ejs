<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-size=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .gradient-bg {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        }
        
        .pulse-animation {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
    </style>
</head>
<body class="gradient-bg min-h-screen flex items-center justify-center">
    <div class="container mx-auto px-4 text-center">
        <div class="max-w-lg mx-auto">
            <!-- Error Icon -->
            <div class="mb-8">
                <div class="inline-block p-6 bg-white bg-opacity-20 rounded-full shadow-lg mb-6 pulse-animation">
                    <i class="fas fa-exclamation-triangle text-6xl text-white"></i>
                </div>
            </div>
            
            <!-- Error Message -->
            <div class="bg-white bg-opacity-10 backdrop-filter backdrop-blur-lg rounded-2xl p-8 shadow-2xl border border-white border-opacity-20">
                <h1 class="text-6xl font-bold text-white mb-4">500</h1>
                <h2 class="text-2xl font-semibold text-white mb-4">Error del Servidor</h2>
                <p class="text-white text-lg opacity-90 mb-8">
                    Algo salió mal en nuestros servidores. Estamos trabajando para solucionarlo.
                </p>
                
                <!-- Error Details (only in development) -->
                <% if (typeof error !== 'undefined' && error && process.env.NODE_ENV === 'development') { %>
                    <div class="bg-red-900 bg-opacity-30 rounded-lg p-4 mb-6 text-left">
                        <h3 class="text-white font-semibold mb-2">Detalles del Error (Desarrollo):</h3>
                        <pre class="text-white text-xs overflow-x-auto whitespace-pre-wrap"><%= error.stack || error.message || error %></pre>
                    </div>
                <% } %>
                
                <!-- Action Buttons -->
                <div class="space-y-4">
                    <button onclick="location.reload()" class="w-full bg-white hover:bg-gray-50 text-gray-700 font-semibold py-3 px-6 rounded-lg shadow-lg transition duration-200 flex items-center justify-center group">
                        <i class="fas fa-sync-alt mr-2"></i>
                        <span>Recargar Página</span>
                    </button>
                    
                    <a href="/dashboard" class="w-full bg-transparent border-2 border-white text-white font-semibold py-3 px-6 rounded-lg hover:bg-white hover:text-gray-700 transition duration-200 flex items-center justify-center group">
                        <i class="fas fa-home mr-2"></i>
                        <span>Ir al Dashboard</span>
                    </a>
                    
                    <button onclick="history.back()" class="w-full bg-transparent border-2 border-white text-white font-semibold py-3 px-6 rounded-lg hover:bg-white hover:text-gray-700 transition duration-200 flex items-center justify-center group">
                        <i class="fas fa-arrow-left mr-2"></i>
                        <span>Volver Atrás</span>
                    </button>
                </div>
            </div>
            
            <!-- Additional Help -->
            <div class="mt-8 text-center">
                <p class="text-white text-sm opacity-75 mb-4">
                    Si el problema persiste, por favor contacta al administrador del sistema.
                </p>
                <div class="flex justify-center space-x-4 text-white text-xs opacity-60">
                    <span>Error ID: <%= Date.now() %></span>
                    <span>•</span>
                    <span>Timestamp: <%= new Date().toISOString() %></span>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Background decoration -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute -top-10 -left-10 w-72 h-72 bg-white opacity-5 rounded-full blur-3xl"></div>
        <div class="absolute top-1/2 -right-10 w-96 h-96 bg-white opacity-5 rounded-full blur-3xl"></div>
        <div class="absolute -bottom-10 left-1/3 w-80 h-80 bg-white opacity-5 rounded-full blur-3xl"></div>
    </div>
    
    <!-- Auto-retry script -->
    <script>
        // Auto-retry after 30 seconds
        setTimeout(() => {
            console.log('Auto-retrying...');
            location.reload();
        }, 30000);
        
        // Show countdown
        let countdown = 30;
        const countdownEl = document.createElement('p');
        countdownEl.className = 'text-white text-sm opacity-60 mt-4';
        countdownEl.textContent = `Reintentando automáticamente en ${countdown} segundos...`;
        document.querySelector('.container').appendChild(countdownEl);
        
        const countdownInterval = setInterval(() => {
            countdown--;
            countdownEl.textContent = `Reintentando automáticamente en ${countdown} segundos...`;
            
            if (countdown <= 0) {
                clearInterval(countdownInterval);
                countdownEl.textContent = 'Reintentando...';
            }
        }, 1000);
    </script>
</body>
</html>
